---
- name: Изменение MOTD
  hosts: all
  become: true

  vars:
    motd_message: "Приветственное сообщение motd!!! Выполнение задание 1.3"

  tasks:
    - name: Изменить приветствие
      ansible.builtin.copy:
        content: "{{ motd_message }}\n"
        dest: /etc/motd
        mode: '0644'
